import Layout from "../components/UI/Layout";
import MainCarousel from "../components/UI/MainCarousel";

const Home = () => {
  return (
    <Layout>
      <MainCarousel />
    </Layout>
  );
};

export default Home;
